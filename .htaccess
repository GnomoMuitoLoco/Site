<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # Se o arquivo ou pasta existir, use-o
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Redirecione tudo para index.php
    RewriteRule ^(.*)$ index.php [L]
</IfModule>
